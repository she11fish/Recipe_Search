import Search from "../../../../utils/search"

interface Props {
    name: string | undefined
    img: string | undefined
    category: string | undefined
    area: string | undefined
}

export default function FoodInfo({ name, img, category, area }: Props) {
    
    return (
        <>
        </>
    )
}